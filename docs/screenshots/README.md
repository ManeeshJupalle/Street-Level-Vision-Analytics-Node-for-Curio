# Screenshots

The README references the following images. Capture and drop into this folder with the exact filenames below — the README links will then resolve automatically.

| Filename | Capture |
|---|---|
| `01_gallery.png` | Standalone frontend at `http://localhost:5173` after a Lincoln Park run completes — Config Panel visible on the left, Gallery grid filled with color-badged result cards on the right |
| `02_inspector.png` | Image Inspector modal open on a single card — show the **CV Overlay** tab and the right-rail **Class Breakdown** chart |
| `03_curio_canvas.png` | Curio canvas (`http://localhost:3000`) with **Street Vision** → **CV Analysis** wired to two **Vega-Lite** boxes |
| `04_map_view.png` | One Vega-Lite box rendered with the **Street Vision — Map View** template — Chicago neighborhoods colored, points overlaid |
| `demo.gif` | 30-second screen capture of the four-step demo (configure → run → inspect → wire). Tools: ScreenToGif (Windows) or `ffmpeg` |
